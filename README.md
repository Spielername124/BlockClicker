This is a Minecraft Plugin made to allow the admin to configure different drops that can be "dropped" by breaking a block.

Currently it is still in developement, hence is downloading the preview only possible by compiling the code yourself.

This plugin is dependend on: WorldEdit, WorldGuard
